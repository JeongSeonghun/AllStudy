package com.jshstudy.allstudy.study.eng;

/**
 * Created by shun6 on 2017-09-05.
 */
// 도치 구문 Ch11 the constructions of inversion
public class Inversion {
    // 가정법
    // 가정법 미래 if 주어 + should + 동원 ~, 주어 + will(can, may, should) + 동원(infinitive, vi)
    // 가정법 과거 if 주어 + 과거 ~, 주어 would(should, could, might) + 동원. 현재 사실에 대한 반대
    // 가정법에서 be는 were만 사용
    // 가정법 과거 완료 if 주어 + had pp ~, 주어 + would(should, could, might) + have + pp. 과거 사실에 대한 반대

    // 도치구문. 뒤에 오는 동사 = 동원
    // 의문문
    // 조동사(조동사 <-> 주어), be동사(be <-> 주어), 일반(do ~)

    // there is/are
    // 주어 <-> 동사

    // if 생략
    // 가정법 미래
    // if you should ~ -> should you ~
    // 가정법 과거완료
    // if he had ~ -> had he ~

    // 부정어
    // hardly, scarcely, seldom, not only, never, neither, only + 부사구)
    // 부정어 동사(일반 do) 주어

    // so do i/ neither do i
    // I am happy, too -> so am I
    // I like him, too -> so do I
    // I am not happy, either -> neither am I
    // I don't like him, either -> neither do I

    // 주어 보어 도치. 주어가 너무 긴 경우

}
