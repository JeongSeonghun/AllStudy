package com.jshstudy.allstudy.study.eng;

/**
 * Created by shun6 on 2017-09-05.
 */
// 전치사 Ch10
public class Preposition {
    // ~에 관하여
    // Regarding, concerning as to, about, on, over

    // ~ 동안
    // for(기간(to month), over, during(기간, 두리뭉실(meeting), 시점(last week)

    // 기간 전치사
    // for, over, ~동안
    // within ~ 이내에 구매관련(purchase, return, exchange, refund)
    // in ~만에
    // throughout + 기간 ~내내 / +지역 ~전역에
    // after ~후에 = following 명사
    // before ~전에 = prior to / previous to 명사

    // by
    //  ~ing ~함으로써
    // 시간 ~까지
    // 목적격 ~에 의하여 수동태에서만쓰임
    // ~self  혼자서
    // 장소, 사람 ~옆에
    // 교통수단 ~로

    // 방향 전치사 to ~쪽으로
    // return과 동사, access to N ~로 접근, 동명사 구문

    // since + 특정 시점, 사건 ~ 이래로
    // since N, 주어 have pp

    // at 시간, 가격, 속도, 장소(~에서)
    // 시간 전치사(at, before, after, until, by, from Ato B, between A and B

    // for
    // ~ 동안(시간)/ 때문에(reason)/ 위해서(명사)

    // over
    // 부사 ~이상 = more than
    // 전치사 ~에 관하여(regarding, concerning, about, as to)
    // 전치사 ~동안

    // on ~에 대해 (regarding, concerning, as to about, on, over)
    // 요일, 특정 날짜

    // in 장소 ~에서
    // 국가, 도시, 년도 달, 계절

    // of ~의 / 중에/ 동격(the city of Seoul)

    // from A to(until) B , A에서 B까지

    // among, between
    // among ~간에, 세명 이상
    // between ~중에 하나, 두명

    // as 접속사 because, when, ~함에 따라 ~하듯이
    // 전치사 ~로서(신분, 수단, 자격)

    // ~시작하여 + 시간,요일, 날짜, 년도, 시점
    // beginning(+on, at, in), starting, as of, effective

    // toward ~즘, 무렵
    // ~을 향해

    // 전치사 + 명사 = 부사

    // ahead of schedule 일정보다 일찍
    // behind of schedule 일정보다 늦게
    // on schedule 일정에 맞춰서

    // in ~ing ~함에 있어서, ~할 때

    // 시간 전치사
    // at, by, until, before(prior to, previous to), after(following)

    // 장소 전치사
    // at, in, in front of, behind, near, next to, close to, beside, by the store

    // ~옆에
    // near, close to, next to, by, beside

    // etc explain
    // according to = in accordance with ~에 따르면
    // in agreement with ~와 함께
    // on behalf of ~을 대신해서, ~을 대표해서
    // in comparison with = compared with, compared to ~와 비교해서
    // in conjunction with ~와 일치하여
    // in cooperation with ~와 협력하여
    // in celebration of ~을 축하하여
    // in honor of ~을 기리며, ~을 축하하여
    // instead of ~대신에

    // 전치사 + 접속사
    // until ~까지(by)
    // before ~전에(전치사 = prior to, previous to)
    // after ~후에(전치사 = following)
    // as ~로서(전치사), ~함에따라(because, when 전치사), ~하듯이(전치사, be pp구문)
    // since ~이래로, ~하기때문에(접속사)

}
