package com.jshstudy.allstudy.study.eng;

/**
 * Created by shun6 on 2017-09-05.
 */

// 접속사 Ch07
    // 명사절 접속사 Ch09
public class Conjunction {

    // 접속사 , 전치사
    // 접속사 문장 잊기
    // 전치사 명사 앞

    // 등위접속사
    // 순접 and, or, nor
    // 인과 so, for
    // 역접 but, yet
    // 특징
    // 앞과 뒤 같은 품사(병치 병렬 구조)
    // 문두에 올 수 없음
    // 앞 뒤 문장 같은 어구 존제시 생략가능
    // 품사 반복시 줄임(N1, N2, and N3)
    // 명령형(should, must 동원) and ~ 해라 그러면 ~할 것이다.
    // 명령형(should, must 동원) or ~ 해라 그렇지 않으면 ~할 것이다.
    // 상관 접속사
    String[] RelativeConjuction = new String[]{
            "both A and B" // 복수 동사
            , "either A or B" // B에 일치
            , "neither A nor B" // ""
            , "not A but B" // ""
            , "not only A but (also) B" // ""
            , "A as well as B" // A에 일치
    };
    // 부사절 이끄는 접속사
    // 접속사 주어 동사
    // 이유
    // because, since, as, now, that
    // 시간
    // when, as, after, until, before
    // as soon as, once
    // since, while
    // every time, whenever
    // by the time
    // 조건
    // if, unless
    // as long as
    // in the event that, in case
    // providing that, provided(that), assuming that
    // supposed, supposing tha, given that considering that, if
    // 양보
    // even though, although, though, even if
    // whereas
    // while
    // 결과
    // so 형,부 that(SVO)너무 ~해서 ~하다
    // such a 형 명 that SVO
    // so that SVO

    // 전치사 명사
    // 이유
    // due to, because of, owing to, thanks to
    // 시간
    // before, prior to , previous to
    // since, until, by
    // after, following, during/for
    // 조건
    // with, without
    // in the event of, in case of
    // 양보
    // despite, in spite of
    // 결과
    // as a result of

    // 부사
    // 조건
    // otherwise
    // 양보
    // however, nevertheless, nonetheless
    // 결과
    // as a result, consequently, therefore
    // thus, hence

    // 분사구문
    // 접속사 생략 및 유지
    // 주어 동일 시 생략
    // 동원 + ing

    // as pp 구문 : ~ 됬듯이
    //
}
